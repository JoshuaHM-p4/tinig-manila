{
  "name": "tinig-manila",
  "version": "1.0.0",
  "private": true,
  "description": "Tinig Manila — Toll-Free AI Voice Hotline for Filipino Senior Citizens (monorepo)",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Tinig Manila Team",
  "license": "MIT"
}
